{
  "schema_version": 2,
  "id": "wf-starter-bugfix",
  "name": "Bugfix Pipeline",
  "nodes": [
    {
      "id": "s-baseline-harness",
      "type": "command",
      "type_version": 1,
      "title": "Measure Harness Baseline",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "capability": "read_only",
        "idempotent": true,
        "max_iterations": 1,
        "measure_baseline": true,
        "prompt_template": null,
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 0.0
      }
    },
    {
      "id": "s-reproduce",
      "type": "agent",
      "type_version": 1,
      "title": "Reproduce & Diagnose",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "artifacts": [
          {
            "capture": {
              "kind": "all_writes"
            },
            "inline": false,
            "mode": "summary_only",
            "name": "changed-files"
          },
          {
            "capture": {
              "kind": "last_write_to",
              "path": "artifacts/s-reproduce.md"
            },
            "inline": false,
            "mode": "full",
            "name": "diagnosis"
          }
        ],
        "capability": "implement",
        "max_iterations": 2,
        "prompt_template": "You are a senior software engineer performing root cause analysis.\n\nBug report: {{feature_description}}\nRepositories in scope: {{repo_list}}\n\n## Your task\n1. Locate the code path that produces this bug. Trace the full execution path from the entry point to the failure.\n2. Write a **minimal reproduction case** — a test, script, or command that reliably triggers the bug and currently fails. Commit it to the feature branch as a failing test (do not apply any fix yet).\n3. State the root cause precisely: which line(s), which logic error, which assumption is violated.\n4. State the boundary of the fix: what files are in scope, what must not be touched.\n\n## Output artifact (required sections)\n- `## Reproduction Case` — the test/script, with instructions to run it\n- `## Execution Trace` — the call stack or execution path that leads to the failure\n- `## Root Cause` — precise, one-paragraph diagnosis\n- `## Fix Boundary` — files in scope + files that must not change\n- `## Risk` — could this fix regress anything else?\n\n## Stop condition\nYou are done when the reproduction case is committed and fails as expected, and all sections are written.\n\nWrite your complete output to `artifacts/s-reproduce.md`.",
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 160.0
      }
    },
    {
      "id": "s-gate-confirm",
      "type": "gate",
      "type_version": 1,
      "title": "Confirm Root Cause Before Fix",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "max_iterations": null,
        "prompt_template": "Review the Diagnosis artifact from 's-reproduce'. The agent has written a reproduction case and identified a root cause. Approve if the diagnosis is correct and the fix boundary is sensible. Redirect if the diagnosis is wrong or the scope is too wide. Cancel to abort.",
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 320.0
      }
    },
    {
      "id": "s-fix",
      "type": "agent",
      "type_version": 1,
      "title": "Apply Fix",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "artifacts": [
          {
            "capture": {
              "kind": "all_writes"
            },
            "inline": false,
            "mode": "summary_only",
            "name": "changed-files"
          }
        ],
        "capability": "implement",
        "prompt_template": "You are a senior software engineer applying a surgical fix.\n\nBug report: {{feature_description}}\nDiagnosis artifact: [attached — from s-reproduce]\n\n## Rules (non-negotiable)\n1. Touch ONLY the files listed in the Fix Boundary section of the diagnosis artifact.\n2. Do NOT refactor unrelated code — one bug, one fix.\n3. Make the reproduction test case pass.\n4. Do NOT break any existing tests.\n5. Run the reproduction test immediately after your fix: {{test_command}}\n\n## Reflection\nAfter applying the fix, re-read the Root Cause section of the diagnosis artifact. Verify your change addresses the stated cause directly. If it does not, revise before declaring done.\n\n## On Retry (only when retry feedback is present)\nIf the following feedback is non-empty, this is attempt {{iteration}} of {{max_iterations}} — verification failed previously:\n{{retry_feedback}}\nFocus exclusively on resolving what the feedback describes; do not redo unrelated work. If the feedback is empty, this is the first attempt — ignore this section.\n\n## Output artifact\n- Files changed (list)\n- The specific lines modified (diff summary)\n- Reproduction test result: PASS / FAIL\n- Confidence: HIGH / MEDIUM / LOW (with reason)\n\n## Stop condition\nYou are done when the reproduction test passes and the output artifact is written.",
        "verifier": null
      },
      "retry": {
        "verdict": {
          "strategy": "redirect",
          "max_attempts": 3,
          "feedback": true,
          "redirect_to": "s-reproduce"
        },
        "agent_failure": {
          "strategy": "redirect",
          "max_attempts": 3,
          "feedback": true,
          "redirect_to": "s-reproduce"
        }
      },
      "position": {
        "x": 0.0,
        "y": 480.0
      }
    },
    {
      "id": "s-smoke",
      "type": "agent",
      "type_version": 1,
      "title": "Smoke Test",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "artifacts": [
          {
            "capture": {
              "kind": "last_write_to",
              "path": "artifacts/s-smoke.md"
            },
            "inline": false,
            "mode": "full",
            "name": "smoke"
          }
        ],
        "capability": "verify",
        "max_iterations": 1,
        "prompt_template": "You are a QA engineer running a targeted smoke test before the full regression suite.\n\nFix artifact: [attached — from s-fix]\n\n## Your task\n0. **Environment setup** — before running any test or build, ensure the project's dependencies are installed.\n   - Detect the package manager / toolchain from repository markers (`package.json` + lockfile, `pyproject.toml` / `requirements*.txt`, `Cargo.toml`, `go.mod`, etc.).\n   - Skip the install only if a marker directory (`node_modules/`, `target/`, `.venv/`, …) already exists for the project.\n   - Run the appropriate install command, preferring locked installs:\n     - Node + `package-lock.json` → `npm ci`\n     - Node + `pnpm-lock.yaml` → `pnpm install --frozen-lockfile`\n     - Node + `yarn.lock` → `yarn install --frozen-lockfile`\n     - Python + `requirements.txt` → `pip install -r requirements.txt`\n     - Python + `pyproject.toml` → `pip install -e .` (or project-defined extras)\n     - Rust → no-op (note in the artifact: `Environment setup: SKIPPED (cargo fetches deps on first test run)`)\n     - Go → `go mod download`\n   - If no package manager is detected, record `Environment setup: NONE (no package manager detected)` and continue.\n   - Record the outcome as a new bullet in the output artifact: `Environment setup: <command> — PASS / FAIL`.\n1. Run the reproduction test case: {{test_command}}. Confirm it passes.\n2. Run the tests for the files touched by the fix.\n3. Run a basic build/compile check: {{build_command}}\n\n## Output artifact\n- Environment setup: <command> — PASS / FAIL (or `SKIPPED` / `NONE` per the rules above)\n- Reproduction test: PASS / FAIL\n- Touched-file tests: X passed, Y failed\n- Build: PASS / FAIL\n- **Proceed to full regression?** YES / NO (with reason)\n\n## Stop condition\nOutput the artifact and stop. Do NOT apply any fixes in this step.\n\nWrite your complete output to `artifacts/s-smoke.md`.",
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 640.0
      }
    },
    {
      "id": "s-verify",
      "type": "agent",
      "type_version": 1,
      "title": "Full Regression Check",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "artifacts": [
          {
            "capture": {
              "kind": "last_write_to",
              "path": "artifacts/s-verify.md"
            },
            "inline": false,
            "mode": "full",
            "name": "verify"
          }
        ],
        "capability": "verify",
        "prompt_template": "You are a QA engineer running the full regression suite. You VERIFY only — you do not modify source code. If the fix introduced a regression, you report it precisely; the fix step addresses it on retry.\n\nSmoke test artifact: [attached — from s-smoke]\n\n## Pre-condition\nCheck the smoke test artifact. If 'Proceed to full regression?' is NO, output a BLOCKED status and stop — do not run the full suite.\n\n## Task (if smoke passed)\nRun the full test suite: {{test_command}}\n\nFor any test that fails:\n1. Determine if the failure is caused by the fix (regression) or was already failing (pre-existing).\n2. If it is a regression: do NOT fix it yourself. Describe precisely which test fails and why, so the fix step can resolve it on retry; mark the overall verdict BLOCKED.\n3. If it is pre-existing: document it in the artifact but do not fix it.\n\n## Output artifact\n- Full suite result: X passed, Y failed, Z skipped\n- Regressions introduced: list (should be empty; describe each precisely if present)\n- Pre-existing failures: list\n- **Overall: READY TO SHIP / BLOCKED**\n\n## Stop condition\nYou are done when the full suite has run and the artifact is written.\n\nWrite your complete output to `artifacts/s-verify.md`.",
        "verifier": {
          "agent_kind": null,
          "instructions": "Decide whether the bug is fixed and no regressions were introduced. The full test-suite output is provided above. Return \"pass\" only if the suite succeeded (no new failures) AND the reproduction case now passes; otherwise return \"fail\" with a concise, actionable reason naming exactly what is still broken.",
          "verdict_key": "verdict"
        }
      },
      "retry": {
        "verdict": {
          "strategy": "redirect",
          "feedback": true,
          "redirect_to": "s-fix"
        },
        "agent_failure": {
          "strategy": "redirect",
          "feedback": true,
          "redirect_to": "s-fix"
        }
      },
      "position": {
        "x": 0.0,
        "y": 800.0
      }
    },
    {
      "id": "s-gate-ship",
      "type": "gate",
      "type_version": 1,
      "title": "Review Fix Before Merging",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "gate_class": "dangerous",
        "max_iterations": null,
        "prompt_template": "Review the Fix artifact (s-fix) and the Regression Check artifact (s-verify). Approve to merge the fix into the feature branch and optionally open an MR. Redirect to re-run a prior step with specific feedback. Cancel to abort.",
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 960.0
      }
    },
    {
      "id": "s-finalize",
      "type": "finalize",
      "type_version": 1,
      "title": "Summarize & open PR",
      "config": {
        "agent_kind": null,
        "allow_network": false,
        "allow_shell": false,
        "max_iterations": null,
        "prompt_template": null,
        "verifier": null
      },
      "position": {
        "x": 0.0,
        "y": 1120.0
      }
    }
  ],
  "edges": [
    {
      "from": "s-baseline-harness",
      "to": "s-reproduce"
    },
    {
      "from": "s-reproduce",
      "to": "s-gate-confirm"
    },
    {
      "from": "s-gate-confirm",
      "to": "s-fix"
    },
    {
      "from": "s-fix",
      "to": "s-smoke"
    },
    {
      "from": "s-smoke",
      "to": "s-verify"
    },
    {
      "from": "s-verify",
      "to": "s-gate-ship"
    },
    {
      "from": "s-gate-ship",
      "to": "s-finalize"
    }
  ]
}
