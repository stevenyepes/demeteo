<!--
  AGENTS.md is the single source of truth for agent instructions in this repo.
  Claude Code reads CLAUDE.md, not AGENTS.md, so this file exists only to import it.
  Keep it to the import — put every instruction in AGENTS.md so opencode, claude-code,
  and hermes all read the same file. HTML comments are stripped before the content
  reaches the model, so this note costs no context.
-->

@AGENTS.md
