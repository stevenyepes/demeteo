{
  "id": "wf-starter-standard",
  "name": "Standard Feature Pipeline",
  "description": "Research the codebase, decompose the feature into small reviewable tickets, write a binding spec, implement the tickets one at a time, validate with tests and security scan, critic-review, and ship. Best for new features of moderate-to-high complexity.",
  "is_starter": true,
  "steps": [
    {
      "id": "s-research",
      "kind": "agent",
      "title": "Research Codebase",
      "agent_kind": null,
      "prompt_template": "You are a senior software architect performing a pre-implementation codebase audit.\n\nThe user wants to build: {{feature_description}}\n\nProject conventions: {{project_conventions}}\nRepositories in scope: {{repo_list}}\n\n## Your task\n1. Identify every file likely to change. For each file: filename, reason, change type (create / modify / delete).\n2. Identify every existing pattern the implementation must follow (naming, error handling, module boundaries, test conventions).\n3. Flag potential risks: circular dependencies, shared state, performance-sensitive paths, security-sensitive areas.\n4. List any external dependencies or APIs the feature will touch.\n\n## Output artifact (required sections, in order)\n- `## Affected Files` — table: filename | change type | reason\n- `## Patterns to Follow` — bullet list with code references\n- `## Risk Register` — table: risk | likelihood (H/M/L) | mitigation\n- `## External Dependencies` — list\n\nWrite this artifact to `artifacts/research-report.md` — create the file. Producing it only as your reply is NOT sufficient; the file must exist.\n\n## Stop condition\nYou are done when `artifacts/research-report.md` exists and contains all four sections and every affected file has a stated reason.",
      "capability": "artifacts",
      "allow_shell": true,
      "artifacts": [
        {
          "name": "research-report",
          "capture": {
            "kind": "last_write_to",
            "path": "artifacts/research-report.md"
          },
          "mode": "full"
        }
      ],
      "on_failure": null,
      "max_iterations": 1
    },
    {
      "id": "s-tickets",
      "kind": "agent",
      "title": "Decompose Into Tickets",
      "agent_kind": null,
      "prompt_template": "You are a technical lead decomposing a feature into an ordered list of tickets — the exact units of work the implementation step will execute, one fresh agent per ticket, in the order you give, each committing before the next starts.\n\nFeature goal: {{feature_description}}\nProject conventions: {{project_conventions}}\nResearch artifact: [attached — s-research]\n\nIf this is a retry, revise the existing `artifacts/task-list.json` in light of the feedback rather than rewriting from scratch.\n\n## Sizing rules (the whole point of this step)\n- Each ticket must be completable by ONE agent in ONE session with a fresh context: small enough that reading the relevant code, implementing, and testing all fit comfortably in a single sitting. Guideline: 1–3 closely related files. If describing a ticket honestly requires \"and then also…\", split it.\n- Prefer vertical slices (tracer bullets): a narrow but COMPLETE path through the layers the ticket touches, demoable or verifiable on its own — not horizontal layers like \"all the types\" or \"all the UI\".\n- Each ticket must be independently verifiable: give it 1–3 binary acceptance criteria and a test command that proves it done.\n- There is NO upper limit on the number of tickets. Emit as many as the sizing rules produce; never merge unrelated work just to shrink the list.\n- Wide refactors are the exception to vertical slicing: use expand–contract sequencing — one ticket adds the new form alongside the old, following tickets migrate call sites in batches, a final ticket deletes the old form.\n\n## Ordering & dependencies\n- Tickets run strictly in the order you give. Order the list so every dependency comes before the ticket that needs it.\n- Declare dependencies explicitly in `blocked_by` (ids of earlier tickets this one builds on). Tickets MAY share files — a later ticket building on an earlier one's work is normal and expected.\n\n## Output (required)\nWrite `artifacts/task-list.json` — create the file. Producing it only as your reply is NOT sufficient; the file must exist.\n\n```json\n{\"tasks\": [{\"id\": \"ticket-1\", \"title\": \"...\", \"description\": \"...\", \"files\": [\"src/foo.rs\"], \"test_command\": \"...\", \"acceptance\": [\"...\"], \"blocked_by\": []}]}\n```\n\n- `id`: kebab-case, unique, stable.\n- `description`: self-contained. The agent implementing it sees the spec, the research report, and the list of already-completed tickets — but none of your reasoning. Name the files, the intended approach, and anything it must not touch.\n- `acceptance`: 1–3 binary pass/fail statements — the ticket's definition of done.\n- `test_command`: the command that proves this ticket is done. Omit it only if the ticket genuinely cannot be tested in isolation.\n- `blocked_by`: ids of earlier tickets this one depends on; `[]` if none.\n\n## Stop condition\nYou are done when `artifacts/task-list.json` exists, parses as the shape above, every ticket satisfies the sizing rules, and the list order respects every `blocked_by` edge.\n\n{{retry_feedback_section}}",
      "capability": "artifacts",
      "allow_shell": true,
      "artifacts": [
        {
          "name": "task-list",
          "capture": {
            "kind": "last_write_to",
            "path": "artifacts/task-list.json"
          },
          "mode": "full"
        }
      ],
      "on_failure": null,
      "max_iterations": 2
    },
    {
      "id": "s-spec",
      "kind": "agent",
      "title": "Draft Implementation Spec",
      "agent_kind": null,
      "prompt_template": "You are a technical lead turning a research report and a ticket decomposition into a binding implementation specification.\n\nFeature goal: {{feature_description}}\nResearch artifact: [attached — s-research]\nTicket list: [attached — s-tickets]\n\n## Your task\nWrite a detailed, actionable spec that the agents implementing the tickets can follow without ambiguity. The ticket list is the execution plan; your spec is the contract every ticket is implemented against. If this is a retry from a previous attempt, revise the existing `artifacts/implementation-spec.md` in light of the user's feedback rather than rewriting from scratch.\n\n## Required spec sections\n1. **Acceptance Criteria** — 3–7 testable, binary-pass/fail statements for the feature as a whole.\n2. **Data Model Changes** — new fields, tables, types, or enum variants with their schema.\n3. **Files to Create or Modify** — from the research artifact, with a one-line change description per file.\n4. **Public API Changes** — new commands, events, or ports; breaking vs additive.\n5. **Testing Strategy** — unit tests required, integration tests, happy path + edge cases.\n6. **Constraints** — things the implementation must NOT do.\n7. **Open Questions** — issues that need user decision before implementation. If the ticket decomposition itself is wrong — a ticket too big for one session, a missing dependency, a wrong order — flag it HERE explicitly instead of silently working around it; the reviewer at the next gate can send the ticket list back for revision.\n\n## Stop condition\nYou are done when all 7 sections are present and the Acceptance Criteria are phrased as testable assertions (not goals).\n\n{{retry_feedback_section}}",
      "capability": "artifacts",
      "allow_shell": true,
      "artifacts": [
        {
          "name": "implementation-spec",
          "capture": {
            "kind": "last_write_to",
            "path": "artifacts/implementation-spec.md"
          },
          "mode": "full"
        }
      ],
      "on_failure": null,
      "max_iterations": 2
    },
    {
      "id": "s-gate-review",
      "kind": "gate",
      "title": "Review Tickets & Spec Before Implementation",
      "agent_kind": null,
      "prompt_template": "Review the Ticket List artifact from step 's-tickets' and the Implementation Spec from step 's-spec'. The ticket list is the exact ordered breakdown the implementation step will execute — one fresh agent per ticket, in order — so this is the moment to fix a bad decomposition, before any code is written. Check that each ticket is small enough for one agent session, independently verifiable, and correctly ordered against its dependencies; check the spec's Open Questions for decomposition problems the spec author flagged. Approve to start implementation. Redirect to 's-tickets' if the decomposition needs revision, or to 's-spec' if the spec does — your feedback will be injected into that step's retry. Cancel to abort the feature.",
      "on_failure": null,
      "max_iterations": null
    },
    {
      "id": "s-implement",
      "kind": "sequence",
      "title": "Implement Tickets",
      "agent_kind": null,
      "prompt_template": "You are an implementation agent. The approved spec [attached — s-spec] and the research report [attached — s-research] describe the feature. It has been broken into an ordered ticket list, and you have been assigned exactly ONE ticket from it.\n\n## Your ticket ({{task_index}} of {{task_total}}): {{task_title}}\n{{task_description}}\n\nFiles you are expected to touch: {{task_files}}\n\n## Acceptance criteria (your definition of done)\n{{task_acceptance}}\n\n## Already done (committed, on your branch, by earlier agents)\n{{completed_tasks}}\n\nYou are working in a worktree that ALREADY CONTAINS the committed work of every ticket above. Read it before you start. Build on it — do not reimplement it, and do not revert it. If a ticket above already did part of what your ticket describes, do the remaining part and say so.\n\n## Rules\n1. Implement your ticket and only your ticket. Later tickets in the list are somebody else's job.\n2. You may modify files outside your expected list if your ticket genuinely requires it — the list is guidance, not a fence. Prefer staying inside it.\n3. Follow the patterns identified in the research artifact and the spec's Constraints section exactly.\n4. Work test-first where the ticket allows: start by writing the failing test that captures an acceptance criterion, then make it pass. Write or update tests for every change you make.\n5. Run the tests before declaring done: {{test_command}}\n6. Do NOT commit. The orchestrator commits your work when your turn ends, and merges the whole ticket list back to the feature branch once every ticket is done.\n\n## Prior review feedback (present only on a re-run)\nIf this feature was already implemented, reviewed, and sent back for rework, the previous reports are attached so you fix exactly what was flagged instead of guessing:\n- Critic review: [attached — s-critic]\n- Validation report: [attached — s-validate]\nOn the first pass these resolve to \"not yet generated\" — ignore them then. On a re-run, treat every Critical / blocking issue that falls within your ticket's scope as part of your ticket, and briefly note any flagged issue that belongs to a different ticket so it is not silently dropped.\n\n{{retry_feedback_section}}\n\n## Stop condition\nYou are done when every acceptance criterion above holds, your tests pass ({{test_command}}), and your changes follow the spec's Constraints.\n\n## Output\n- What you changed, and why\n- Each acceptance criterion: MET / NOT MET\n- Test results (pass/fail counts)\n- Anything a later ticket needs to know",
      "capability": "implement",
      "on_failure": "s-gate-review",
      "max_iterations": 2,
      "artifacts": [
        {
          "name": "implemented-files",
          "capture": {
            "kind": "all_writes"
          },
          "mode": "summary_only"
        }
      ],
      "task_list_from": "s-tickets"
    },
    {
      "id": "s-validate",
      "kind": "agent",
      "title": "Validate, Test & Security Scan",
      "agent_kind": null,
      "prompt_template": "You are a QA engineer running the final validation gate before this feature merges. You VERIFY only — you do not modify source code, and you do NOT re-run the build or tests: the orchestrator has already executed the project's test harness in this worktree and its results are appended to this prompt under 'Harness Results'. Treat that output as authoritative.\n\nFeature branch: {{feature_branch}}\nSpec artifact — the acceptance criteria you must verify against live here: [attached — s-spec]\nImplementation ticket summaries: [attached — all s-implement summaries]\n\n## Validation sequence\n1. **Interpret the harness output** — summarize build/test results (pass/fail counts, failing tests verbatim).\n2. **Review the change** — inspect the implementation diff (`git diff` / `git log` are available read-only) against the spec artifact's acceptance criteria. Check each criterion explicitly.\n3. **Scan for red flags** — hardcoded secrets, obviously unhandled error paths, TODOs left in shipped code. Read files as needed; do not execute build tooling.\n\n## Output artifact (write to artifacts/validation-report.md)\n- Harness: summary of the provided results\n- Acceptance criteria: one line per criterion, MET / NOT MET with evidence\n- Red flags: list, or 'none'\n- **Overall: READY TO SHIP / BLOCKED**\n\n## Stop condition\nYou are done when the artifact contains all sections and an Overall verdict, and your reply ends with the required verdict JSON described below.",
      "capability": "verify",
      "artifacts": [
        {
          "name": "validation-report",
          "capture": {
            "kind": "last_write_to",
            "path": "artifacts/validation-report.md"
          },
          "mode": "full"
        }
      ],
      "on_failure": "s-implement",
      "max_iterations": null,
      "verifier": {
        "agent_kind": null,
        "instructions": "Decide whether the implementation satisfies the feature goal and every acceptance criterion in the spec. The harness output above is authoritative and already succeeded (a failing harness never reaches you). Return \"pass\" only if the changes are also correct and complete against the spec; otherwise return \"fail\" with a concise, actionable reason naming exactly what to fix, the failing_tests (if any), and the implicated_files that must change.",
        "harness_name": null,
        "verdict_key": "verdict"
      }
    },
    {
      "id": "s-critic",
      "kind": "agent",
      "title": "Critic Review",
      "agent_kind": null,
      "prompt_template": "You are a senior code reviewer with an adversarial mindset. Your role is to find problems — not to be kind.\n\nFeature goal: {{feature_description}}\nSpec artifact: [attached — s-spec]\nImplementation ticket artifacts: [attached — all s-implement summaries]\nValidation report (build and tests already passed): [attached — s-validate]\n\n## Review dimensions (check each explicitly)\n1. **Correctness** — does the implementation satisfy every acceptance criterion?\n2. **Spec compliance** — are there deviations from the spec's constraints?\n3. **Security** — hardcoded secrets, injection risks, path traversal, TOCTOU vulnerabilities.\n4. **Performance** — N+1 queries, unbounded loops, unnecessary allocations on hot paths.\n5. **Test coverage** — are edge cases and error paths tested?\n6. **Code quality** — naming, duplication, dead code, overly complex logic.\n\n## Output artifact (required)\n- `## Critical Issues` — MUST be fixed before ship (blocking)\n- `## Major Issues` — should be fixed (non-blocking but important)\n- `## Minor Issues` — nice-to-fix\n- `## Verdict` — one of: `PASS`, `PASS_WITH_NOTES`, `FAIL`\n\nWrite this review to `artifacts/critic-review.md` — create the file. Producing it only as your reply is NOT sufficient; the file must exist.\n\n## Stop condition\nYou are done when `artifacts/critic-review.md` exists with all 6 dimensions assessed and a Verdict written.",
      "capability": "artifacts",
      "allow_shell": true,
      "artifacts": [
        {
          "name": "critic-review",
          "capture": {
            "kind": "last_write_to",
            "path": "artifacts/critic-review.md"
          },
          "mode": "full"
        }
      ],
      "on_failure": null,
      "max_iterations": 1
    },
    {
      "id": "s-gate-ship",
      "kind": "gate",
      "gate_class": "dangerous",
      "title": "Approve Merge / Publish",
      "agent_kind": null,
      "prompt_template": "Review the Validation artifact (s-validate) and the Critic Review (s-critic). Approve to merge the feature branch and optionally open an MR. Redirect to reopen a prior step with specific feedback. Cancel to abort.",
      "on_failure": null,
      "max_iterations": null
    },
    {
      "id": "s-finalize",
      "kind": "finalize",
      "title": "Summarize & open PR",
      "agent_kind": null,
      "prompt_template": null,
      "on_failure": null,
      "max_iterations": null
    }
  ]
}
